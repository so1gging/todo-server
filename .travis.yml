language: java
jdk:
  - openjdk8

branches: # 1.
  only:
    - master

# Travis CI 서버의 Home
cache: # 2.
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build" # 3.